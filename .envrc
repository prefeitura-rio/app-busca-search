#!/usr/bin/env bash

# Enable Nix flakes
use flake

# Load environment variables from .env file if it exists
dotenv_if_exists .env

# Notify when environment is loaded
echo "🔧 Development environment activated via direnv"